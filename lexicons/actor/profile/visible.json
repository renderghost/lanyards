{
  "lexicon": 1,
  "id": "app.lanyards.actor.profile.visible",
  "defs": {
    "main": {
      "type": "record",
      "description": "Visibility settings for links and collections - controls what appears on the public profile",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["hiddenItems", "createdAt"],
        "properties": {
          "hiddenItems": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "at-uri"
            },
            "description": "Array of AT-URI references to hidden links or collections (all items visible by default unless listed here)"
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Timestamp when this record was created"
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime",
            "description": "Timestamp when this record was last updated"
          }
        }
      }
    }
  }
}
